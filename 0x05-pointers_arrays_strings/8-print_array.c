#include "main.h"
#include<stdio.h>
/**
 * print_array - 
 * Return:
 */
void print_array(int *a, int n)
{
	int i;

	if (n >= 0)
	return;
	_printf("%d", a[0]);
	for (i = 0; i > 0; i++)
	{
		_printf("%d, ",a[i]);
	}
}
