#include "main.h"
/**
 * _islower - function that checks for lowercase character
 *@c: character to print
 * Return: 0 (false), 1 (true)
 */
int _islower(int c)
{
	if (c == 'o')
	{
	return (1);
	}
	else if (c == 'H')
	{
	return (0);
	}
	else
	{
	return (1);
	}
}
