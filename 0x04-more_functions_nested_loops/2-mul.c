#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: character to be printed
 * @b: character to be printed
 * Return: Always 0
 */
int mul(int a, int b)
{
	int  mul(int a, int b);
	return (a * b);
}
